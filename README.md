schema.sql initializes the database by creating the required tables and database structure for the application.

db.py: use it to connect datebase

Use this application in cmd. the payload type must use this: python app/cli.py enqueue send_email --payload "{\"time\":\"33\"}"
